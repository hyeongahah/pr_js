// 삼항 연산 표현 방법
let favorite = 'apple';
let pick = favorite == 'apple' ? 'yes' : 'no';

console.log(pick); // yes

let num = 3;
let whatsNum = num / 2 == 0 ? 'even number' : 'odd number';
console.log(whatsNum); //odd number
