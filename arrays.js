/** 새 배열 만들기
 * new Array() 생성자 함수로 만들기
 * Array.of static 함수로 만들기
 */

/** 기존 배열에 영향
 * indexof 특정 요소의 위치 찾기
 * includes 특정 요소가 있는지 확인
 * push 맨 뒤 추가
 * unshift 맨 앞 추가
 * pop 맨 뒤 삭제
 * shift 맨 앞 삭제
 * splice(삭제 시작 위치, 개수, 추가 요소)
 * fill(채울 문자, 시작, 미만)
 * join(합칠 형태) 배열 내의 요소들을 지정한 형태로 합침
 */

/** 새로운 배열로 재생성
 * slice(시작, 미만)  // 범위를 잘라낸다
 * array1.concat(array2) 두 배열을 그대로 합친다
 * reverse() 배열 순서를 거꾸로 나열
 * flat(단계 수) 배열 중복을 없애줌
 */
/** 기존 객체의 메모리 주소를 저장
 * Array.from(요소를 가져올 배열)
 * [object1, object2] 배열 리터럴로 만들기
 */
